{
    "version": 2,
    "buildCommand": "cd pos-terminal && npm install && npm run build",
    "outputDirectory": "pos-terminal/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/pos-terminal/dist/index.html"
        }
    ]
}